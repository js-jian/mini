export enum RouteEnum {
  SIGN_UP = "SIGN_UP",
}

export enum ErrorCodeEnum {
  SUCCESS = 200,
  SERVICE_ERR = 500,
}

export enum CollectionEnum {
  JIE_NO_COLLECTION = "no-collection",
  JIE_SETUP = "jie_setup",
}
